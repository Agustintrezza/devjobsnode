<main class="contenedor">
    <form action="/vacantes/nueva" method="POST" class="default-form">
        <h3>Información General</h3>

        <div class="campo">
            <label>Titulo</label>
            <input type="text" name="titulo" placeholder="Ej: Angular Developer">
        </div>

        <div class="campo">
            <label>Empresa</label>
            <input type="text" name="empresa" placeholder="Empresa Contrata">
        </div>
        <div class="campo">
            <label>Ubicación</label>
            <input type="text" name="ubicacion" placeholder="Ej: Argentina o Remoto">
        </div>
        <div class="campo">
            <label>Salario</label>
            <input type="text" name="salario" placeholder="Ej: $xxx">
        </div>

        <div class="campo">
            <label>Contrato</label>
            <select name="contrato">
                <option value="" disabled selected> -- Selecciona --</option>
                <option value="Freelance">Freelance</option>
                <option value="Full Time">Full Time</option>
                <option value="Part Time">Part Time</option>
                <option value="Por Proyecto">Por Proyecto</option>
            </select>
        </div>
        <div class="campo">
            <label>Contacto</label>
            <input type="email" name="contacto" placeholder="Email de contacto">
        </div>
        <h3>Descripción del Puesto</h3>

        <div class="campo descripcion">
            <label> Descripción </label>
            <input type="hidden" id="x" name="descripcion">
            <trix-editor input="x"></trix-editor>
        </div>

        <h3>Conocimientos</h3>
        <ul class="lista-conocimientos">
            {{#seleccionarSkills a }}
            {{/seleccionarSkills}}
        </ul>

        <div clasS="campor centrar-horizontal">
            <input type="hidden" name="skills" id="skills">
            <input type="submit" value="Publicar" class="btn btn-azul">
        </div>

    </form>

</main>