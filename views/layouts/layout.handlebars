<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@300;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" integrity="sha512-KfkfwYDsLkIlwQp6LFnl8zNdLGxu9YAA1QvwINks4PhcElQSvqcyVLLD9aMhXd13uQjoXtEKNosOWaZqXgel0g==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/css/trix.css" />
    <link rel="stylesheet" href="/css/app.css" />
    <title>{{nombrePagina}}</title>
  </head>
  <body>
    <div class="nombre-sitio contenedor">
  
      <h1>
          <a class="prueba" href="/">devJobs</a>
      </h1>
       
       {{#if cerrarSesion2}}
        <div class="cerrar-sesion">
          <a href="/iniciar-sesion" >Iniciar Sesión</a>
          <a href="/crear-cuenta" >Crear Cuenta</a>
        </div>
      {{/if}}
        {{#if barra}}
            <div class="buscador">
                <form action="/buscador" method="POST">
                    <input type="text" name="q" class="buscar">
                    <input type="submit" value="Buscar">
                </form>
                {{!-- {{#if iniciarSesion}}
                <a href="/iniciar-sesion">Iniciar Sesión</a>
                {{/if}} --}}
            </div>
        {{/if}} 
        {{#if cerrarSesion}}
          <div class="cerrar-sesion">
              <i class="fa-solid fa-user"></i>
              <p>Hola {{nombre}}</p>
              <a href="/administracion">Administración</a>
              <a href="/cerrar-sesion">Cerrar Sesión</a>
          </div>
        {{/if}}
    </div>

        <header class="site-header contenedor separador">

            <h2>{{nombrePagina}}</h2>

            {{#if tagline}}
                <p class="tagline">{{tagline}}</p>
            {{/if}}
            {{#if boton}}
                <a href="/vacantes/nueva" class="btn btn-azul">Publicar Nueva Vacante</a>
            {{/if}}
        </header>

      <div class="contenido-principal contenedor">
        {{#if mensajes}}
          <div class="alertas">
              {{#mostrarAlertas mensajes}}
              {{/mostrarAlertas}}
          </div>
        {{/if}}
          {{{body}}}
      </div>

      <script src="/js/trix.js"></script>
      <script src="/dist/bundle.js"></script>
  </body>
</html>